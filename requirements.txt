# Runtime dependencies for the verification core (Phase 0).
# Deliberately minimal: the no-egress fallback (N-06) means the core must not
# depend on any network service or heavyweight ML stack.
Pillow>=10,<13

# The three AI features (DESIGN.md 2.9). Used only when ANTHROPIC_API_KEY is
# set: with no key the app runs identically minus those features, which is a
# supported configuration (N-06) and what the whole test suite exercises.
anthropic>=0.40

# API service (Phase 1).
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
